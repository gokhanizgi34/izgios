@extends('layouts.app')
@section('title', 'Şifre Yenileme Talepleri')
@section('content')
<section class="container py-2"><div class="page-header"><div><h1>Şifre Yenileme Talepleri</h1><p>İnsan Kaynakları Merkezi içindeki güvenli şifre talep kuyruğu.</p></div><div class="d-flex gap-2"><a class="btn btn-secondary" href="{{ route('ik.index') }}">İK Merkezine Dön</a><a class="btn btn-outline-primary" href="{{ route('ik.iletisim') }}">İK E-posta Ayarı</a></div></div>@if(session('success'))<div class="alert alert-success">{{ session('success') }}</div>@endif<div class="card"><div class="card-body table-responsive"><table class="table"><thead><tr><th>Kullanıcı</th><th>Talep zamanı</th><th>Durum</th><th>İşlem</th></tr></thead><tbody>@forelse($talepler as $talep)<tr><td><strong>{{ $talep->kullanici?->tamAdi() }}</strong><small class="d-block text-muted">{{ $talep->istek_email }}</small></td><td>{{ $talep->created_at->format('d.m.Y H:i') }}</td><td>{{ ucfirst($talep->durum) }}</td><td>@if($talep->durum === 'bekliyor')<form method="POST" action="{{ route('ik.sifre.onayla', $talep) }}">@csrf @method('PATCH')<button class="btn btn-sm btn-primary">Onayla ve bağlantı gönder</button></form>@else<small class="text-muted">{{ $talep->onaylandi_at?->format('d.m.Y H:i') }}</small>@endif</td></tr>@empty<tr><td colspan="4" class="text-center">Şifre yenileme talebi bulunmuyor.</td></tr>@endforelse</tbody></table></div></div></section>
@endsection
