@extends('layouts.app')
@section('title','API ve Entegrasyon Ayarları')
@section('content')<section class="container py-2"><div class="page-header"><div><h1><i class="bi bi-plug-fill"></i> API ve Entegrasyon Ayarları</h1><p>Her firma kendi muhasebe, e-fatura ve yapay zekâ bağlantı anahtarını güvenli olarak yönetir.</p></div></div>@if(session('success'))<div class="alert alert-success">{{ session('success') }}</div>@endif<div class="card"><div class="card-body p-4"><form method="GET"><label class="form-label">Firma</label><select name="firma_id" onchange="this.form.submit()">@foreach($firmalar as $firma)<option value="{{ $firma->id }}" @selected($firmaId==$firma->id)>{{ $firma->gosterim_adi }}</option>@endforeach</select></form><hr>@foreach(['logo'=>'Logo Muhasebe','parasut'=>'Paraşüt','efatura'=>'E-Fatura Sağlayıcısı','openai'=>'OpenAI Yapay Zekâ'] as $kod=>$ad)@php($hazir=($entegrasyonlar[$kod]->durum ?? '')==='yapilandirildi'||($kod==='openai'&&$openAiGlobal))<form class="border rounded p-3 mb-3" method="POST" action="{{ route('ticari.api.kaydet') }}">@csrf<input type="hidden" name="firma_id" value="{{ $firmaId }}"><input type="hidden" name="saglayici" value="{{ $kod }}"><div class="d-flex justify-content-between"><strong>{{ $ad }}</strong><span class="badge text-bg-{{ $hazir?'success':'secondary' }}">{{ $hazir?'Yapılandırıldı':'Yapılandırılmadı' }}</span></div><label class="form-label mt-2">API Anahtarı</label><input type="password" name="api_anahtari" placeholder="{{ ($entegrasyonlar[$kod]->api_anahtari_sifreli ?? null)?'••••••••  kayıtlı anahtar korunur':'API anahtarını girin' }}"><small class="d-block text-muted mt-1">Anahtar şifrelenerek saklanır ve hiçbir ekranda açık gösterilmez.</small><button class="btn btn-outline-primary btn-sm mt-2">Kaydet</button></form>@endforeach</div></div></section>@endsection
