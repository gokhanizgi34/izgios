@extends('layouts.app')
@section('title','Muhasebe Fişleri')
@section('content')
<section class="container"><div class="page-header"><div><h1>Muhasebe Fişleri</h1><p>İşletme masraflarını, gelirleri ve servis dışı harcamaları fiş olarak işleyin.</p></div></div>@if(session('success'))<div class="alert alert-success">{{ session('success') }}</div>@endif<form method="GET" class="card mb-3"><div class="card-body"><label>Firma</label><select name="firma_id" onchange="this.form.submit()">@foreach($firmalar as $firma)<option value="{{ $firma->id }}" @selected($firmaId==$firma->id)>{{ $firma->gosterim_adi }}</option>@endforeach</select></div></form><div class="row g-3"><div class="col-lg-5"><div class="card"><div class="card-body"><h2 class="h5">Yeni Fiş</h2><form method="POST">@csrf<input type="hidden" name="firma_id" value="{{ $firmaId }}"><label>Fiş yönü</label><select name="yon"><option value="gider">Gider / Masraf</option><option value="gelir">Gelir</option></select><label>Fiş türü</label><input name="tip" value="Genel Gider" required><label>Cari hesap</label><select name="cari_hesap_id"><option value="">Cari seçilmedi</option>@foreach($cariler as $cari)<option value="{{ $cari->id }}">{{ $cari->unvan }}</option>@endforeach</select><label>Tarih</label><input type="date" name="fis_tarihi" value="{{ now()->format('Y-m-d') }}" required><label>Açıklama</label><textarea name="aciklama" required></textarea><label>Tutar</label><input type="number" step=".01" min="0.01" name="tutar" required><button class="btn btn-primary mt-3">Fişi İşle</button></form></div></div></div><div class="col-lg-7"><div class="card"><div class="card-body"><h2 class="h5">İşlenen Fişler</h2>@forelse($fisler as $fis)<div class="border-bottom py-2"><b>{{ $fis->fis_no }}</b><span class="float-end {{ $fis->yon==='gelir'?'text-success':'text-danger' }}">₺{{ number_format($fis->tutar,2,',','.') }}</span><small class="d-block">{{ $fis->fis_tarihi }} · {{ $fis->tip }} · {{ $fis->cari_unvan ?: 'Genel işletme' }}</small><span>{{ $fis->aciklama }}</span></div>@empty<p class="text-muted">Henüz fiş işlenmedi.</p>@endforelse</div></div></div></div></section>
@endsection
