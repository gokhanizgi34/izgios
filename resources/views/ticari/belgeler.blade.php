@extends('layouts.app')
@section('title',ucfirst($tur).'ler')
@section('content')<section class="container py-2"><div class="page-header"><div><h1>{{ $tur==='teklif'?'Teklifler':'Faturalar' }}</h1><p>Firma bazlı {{ $tur }} kayıt ve takip ekranı.</p></div></div>@if(session('success'))<div class="alert alert-success">{{ session('success') }}</div>@endif<div class="card mb-3"><div class="card-body p-4"><form method="POST" action="{{ route('ticari.belge.kaydet',$tur) }}">@csrf<div class="row g-3"><div class="col-md-4"><label class="form-label">Firma</label><select name="firma_id">@foreach($firmalar as $firma)<option value="{{ $firma->id }}" @selected($firmaId==$firma->id)>{{ $firma->gosterim_adi }}</option>@endforeach</select></div><div class="col-md-4"><label class="form-label">Müşteri / Firma</label><input name="musteri_unvan" required></div><div class="col-md-2"><label class="form-label">Tarih</label><input type="date" name="tarih" value="{{ now()->format('Y-m-d') }}" required></div><div class="col-md-2"><label class="form-label">Tutar</label><input type="number" step=".01" name="tutar" required></div>@if($tur==='teklif')<div class="col-12"><label class="form-label">Açıklama</label><textarea name="aciklama"></textarea></div>@endif</div><button class="btn btn-primary mt-3">{{ $tur==='teklif'?'Teklif Oluştur':'Fatura Kaydet' }}</button></form></div></div><div class="card"><div class="card-body p-4"><h2 class="h5">Kayıtlar</h2>@forelse($kayitlar as $kayit)<div class="border-bottom py-3 d-flex justify-content-between"><span><strong>{{ $tur==='teklif'?$kayit->teklif_no:$kayit->fatura_no }}</strong><br><small>{{ $kayit->musteri_unvan }} · {{ $kayit->tarih }}</small></span><strong>₺ {{ number_format($kayit->tutar,2,',','.') }}</strong></div>@empty<p class="text-muted">Henüz kayıt yok.</p>@endforelse</div></div></section>@endsection
