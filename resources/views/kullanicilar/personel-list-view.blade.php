@extends('layouts.app')
@section('title', $durum === 'aktif' ? 'Aktif Personel' : 'Pasif Personel')
@section('content')
<section class="container py-2"><div class="page-header"><div><h1>{{ $durum === 'aktif' ? 'Aktif Personel' : 'Pasif Personel' }}</h1><p>Personel kayıtları durumlarına göre ayrı ekranlarda tutulur.</p></div><a class="btn btn-primary" href="{{ route('kullanicilar.create') }}">Yeni Personel</a></div><div class="d-flex gap-2 mb-3"><a class="btn {{ $durum === 'aktif' ? 'btn-primary' : 'btn-secondary' }}" href="{{ route('kullanicilar.aktifler') }}">Aktif Personel</a><a class="btn {{ $durum === 'pasif' ? 'btn-primary' : 'btn-secondary' }}" href="{{ route('kullanicilar.pasifler') }}">Pasif Personel</a></div>@if(session('success'))<div class="alert alert-success">{{ session('success') }}</div>@endif @if(session('error'))<div class="alert alert-danger">{{ session('error') }}</div>@endif<div class="card"><div class="card-body table-responsive"><table class="table"><thead><tr><th>Personel</th><th>İletişim</th><th>Rol</th><th>Durum</th><th>İşlem</th></tr></thead><tbody>@forelse($kullanicilar as $kullanici)<tr><td><strong>{{ $kullanici->tamAdi() }}</strong><small class="d-block text-muted">{{ $kullanici->email }}</small></td><td>{{ $kullanici->phone ?: '-' }}</td><td>{{ $kullanici->rolAdi() }}</td><td>{{ ucfirst($kullanici->status) }}</td><td><a class="btn btn-sm btn-outline-primary" href="{{ route('kullanicilar.edit', $kullanici) }}">Düzenle</a></td></tr>@empty<tr><td colspan="5" class="text-center">Bu listede personel bulunmuyor.</td></tr>@endforelse</tbody></table></div></div></section>
@endsection
