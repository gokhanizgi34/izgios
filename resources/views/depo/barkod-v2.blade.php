@extends('layouts.app')
@section('title','Barkod ve Raf Adresleme')
@section('content')
<section class="container" style="max-width:1300px"><div class="p-4 rounded text-white" style="background:linear-gradient(120deg,#172554,#0f766e)"><h1 class="h3"><i class="bi bi-upc-scan"></i> Barkod ve Raf Adresleme</h1><p class="mb-0">Depo → raf → ürün bağlantısını kurun; ürün ve raf adresi etiketlerini yazdırın.</p></div>@if(session('success'))<div class="alert alert-success mt-3">{{ session('success') }}</div>@endif<div class="row g-3 mt-1"><div class="col-md-4"><div class="card"><div class="card-body"><h2 class="h5">1. Depo</h2><form method="POST" action="{{ route('depo.depo.kaydet') }}">@csrf<input name="ad" placeholder="Ana Depo" required><button class="btn btn-primary mt-2">Depo Oluştur</button></form></div></div></div><div class="col-md-4"><div class="card"><div class="card-body"><h2 class="h5">2. Raf Adresi</h2><form method="POST" action="{{ route('depo.raf.kaydet') }}">@csrf<select name="depo_id" required><option value="">Depo seçiniz</option>@foreach($depolar as $depo)<option value="{{ $depo->id }}">{{ $depo->ad }}</option>@endforeach</select><input class="mt-2" name="kod" placeholder="A-01-03" required><button class="btn btn-primary mt-2">Raf Oluştur</button></form></div></div></div><div class="col-md-4"><div class="card"><div class="card-body"><h2 class="h5">3. Ürünü Adresle</h2><form method="POST" action="{{ route('depo.raf.ata') }}">@csrf<select name="stok_parca_id" required><option value="">Ürün seçiniz</option>@foreach($parcalar as $parca)<option value="{{ $parca->id }}">{{ $parca->parca_adi }}</option>@endforeach</select><select class="mt-2" name="depo_raf_id" required><option value="">Raf seçiniz</option>@foreach($raflar as $raf)<option value="{{ $raf->id }}">{{ $raf->depo_adi }} / {{ $raf->kod }}</option>@endforeach</select><button class="btn btn-success mt-2">Rafa Bağla</button></form></div></div></div></div><div class="card mt-3"><div class="card-body table-responsive"><h2 class="h5">Ürün ve Raf Etiketleri</h2><table class="table"><thead><tr><th>Barkod</th><th>Ürün</th><th>Raf adresi</th><th>Etiket</th></tr></thead><tbody>@forelse($parcalar as $p)<tr><td><strong>{{ $p->barkod ?: $p->oem_no }}</strong></td><td>{{ $p->parca_adi }}<small class="d-block">OEM: {{ $p->oem_no }}</small></td><td>{{ $p->raf_kodu ? $p->depo_adi.' / '.$p->raf_kodu : 'Adreslenmemiş' }}</td><td><button class="btn btn-sm btn-outline-primary" onclick="window.print()"><i class="bi bi-printer"></i> Etiket Yazdır</button></td></tr>@empty<tr><td colspan="4">Ürün bulunamadı.</td></tr>@endforelse</tbody></table></div></div></section>
@endsection
